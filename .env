# MongoDB Docker Compose Environment Variables
# Used by docker-compose.yml

# MongoDB Credentials (LOCAL DEVELOPMENT ONLY)
# For production, use secure credentials from environment variables
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=change_me_in_production
MONGO_DATABASE=studencollabfin

# MongoDB Express Settings (LOCAL DEVELOPMENT ONLY)
# For production, disable or use secure credentials
MONGO_EXPRESS_LOGIN=admin
MONGO_EXPRESS_PASSWORD=change_me_in_production

# Backend Environment (optional, if using backend service in docker-compose)
SPRING_PROFILES_ACTIVE=dev
JWT_SECRET=change_me_in_production_use_strong_random_key
