# application-dev.properties
# Local Development Profile
# To use this profile, run: mvn spring-boot:run -Dspring.profiles.active=dev
# OR run MongoDB via: docker-compose up

# Server Configuration
server.port=8080

# JWT Configuration - Local Testing Value
jwt.secret=StudCollabDefault2026

# MongoDB Atlas Configuration for Local Debugging
# Replace PASSWORD with the one used in your Render Environment Variables
spring.data.mongodb.uri=mongodb+srv://render_app:RenderApp2026@finiq.mukfozh.mongodb.net/studencollabfin?retryWrites=true&w=majority&authSource=admin
spring.data.mongodb.database=studencollabfin

# File Upload Configuration - Local
file.upload.dir=${user.dir}/uploads

# Multipart file upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.mongodb.driver=INFO
