# JWT Configuration
# This key must be long (32+ chars) to be secure for HS256
# Using environment variable for production security
# Default fallback for development: StudCollabDefault2026
jwt.secret=${JWT_SECRET:StudCollabDefault2026}

# MongoDB Atlas Configuration
# Using environment variable for password security
spring.data.mongodb.uri=mongodb+srv://diptan0506:${MONGO_DB_PASSWORD}@finiq.mukfozh.mongodb.net/studencollabfin?retryWrites=true&w=majority
spring.data.mongodb.database=studencollabfin

# File Upload Configuration
file.upload.dir=${user.dir}/uploads

# Multipart file upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

