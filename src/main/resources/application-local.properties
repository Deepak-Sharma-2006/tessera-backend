# Local Development Configuration
# This file is for local development overrides (DO NOT COMMIT sensitive data)

# MongoDB Atlas Credentials (set via environment variable)
# To run locally with Atlas: set MONGODB_URI with your Atlas connection string before starting the app
# Example: 
#   Windows: set MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname && mvn spring-boot:run
#   Linux/Mac: MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname mvn spring-boot:run

# JWT Secret (optional for development, required for production)
# Example:
#   Windows: set JWT_SECRET=your-secure-secret-key-min-32-chars && mvn spring-boot:run
#   Linux/Mac: JWT_SECRET=your-secure-secret-key-min-32-chars mvn spring-boot:run

# For local testing with LOCAL MongoDB (not Atlas):
# spring.data.mongodb.uri=mongodb://localhost:27017/tessera_dev
# spring.data.mongodb.database=tessera_dev
# jwt.secret=local-development-secret-key-only

# IMPORTANT: Always use environment variables for production credentials!
