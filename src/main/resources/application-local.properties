# Local Development Configuration
# This file is for local development overrides (DO NOT COMMIT sensitive data)

# MongoDB Atlas Credentials (set via environment variable)
# To run locally with Atlas: set MONGO_DB_PASSWORD=your_password before starting the app
# Example: 
#   Windows: set MONGO_DB_PASSWORD=your_password && mvn spring-boot:run
#   Linux/Mac: MONGO_DB_PASSWORD=your_password mvn spring-boot:run

# JWT Secret (optional for development, required for production)
# Example:
#   Windows: set JWT_SECRET=your_secret_key && set MONGO_DB_PASSWORD=your_password && mvn spring-boot:run
#   Linux/Mac: JWT_SECRET=your_secret_key MONGO_DB_PASSWORD=your_password mvn spring-boot:run

# For local testing without environment variables, you can temporarily uncomment:
# spring.data.mongodb.uri=mongodb+srv://diptan0506:YOUR_PASSWORD@finiq.mukfozh.mongodb.net/studencollabfin?retryWrites=true&w=majority
# spring.data.mongodb.database=studencollabfin
# jwt.secret=your_development_secret_key
